[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

# See more config options https://github.com/foundry-rs/foundry/tree/master/config

[invariant]
fail_on_revert = false
runs = 5000 #you can increase the runs for better result
#depth = 23